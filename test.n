import "lexer"
import "parser"
import "interpreter"

source = "x = 1 + 2 * 3\nprint x"
tokens = lexer.tokenize(source)
result = parser.parse(tokens, 0)
ast = result[0]
env = interpreter.envNew(0)
interpreter.execBlock(ast, env)